{
    "private": true,
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "seed": "vendor/bin/sail artisan migrate:refresh --seed",
        "document": "vendor/bin/sail artisan l5-swagger:generate"
    },
    "devDependencies": {
        "laravel-vite-plugin": "^0.7.2",
        "vite": "^4.0.0"
    },
    "dependencies": {
        "@prettier/plugin-php": "^0.19.4",
        "prettier": "^2.8.8"
    }
}
